<% content_for :body_content_title do %>
  <%= @portfolio_item.title %>
<% end %>

<% content_for :body_content_left do %>
  <section id="portfolio_item_image" class="portfolio_item">  
    <p>
      <%= link_to image_fu(@portfolio_item.screenshot, '600x800'), @portfolio_item.link %>
    </p>
  </section> 
  <section id="portfolio_item_description" class="portfolio_item">
    <p>
      <%=raw @portfolio_item.description %>
    </p>
  </section>
  <section id="portfolio_item_link" class="portfolio_item">
    Link: <%= link_to @portfolio_item.link, @portfolio_item.url %>
  </section>
  <section id="portfolio_item_date" class="portfolio_item">
    <%= t(:published_at).capitalize %> <%=l @portfolio_item.published_at.to_date, :format => :long %>
  </section>
<% end %>

<% content_for :body_content_right do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="portfolio_items">
      <% @portfolio_items.each do |portfolio_item| %>
        <li>
          <%= link_to image_tag(portfolio_item.screenshot.thumbnail('150x100#').url)+portfolio_item.title.truncate(20, :separator => ' '), portfolio_item_path(portfolio_item) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render :partial => "/shared/content_page" %>
